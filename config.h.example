#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration
// Replace with your actual WiFi credentials
const char* WIFI_SSID = "YOUR_WIFI_SSID_HERE";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD_HERE";

// MQTT Configuration  
const char* MQTT_SERVER = "broker.hivemq.com";
const int MQTT_PORT = 1883;
const char* MQTT_TOPIC = "your/topichere";
const char* MQTT_CLIENT_ID = "arduino_car_client";

// OpenAI Configuration
const char* OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE";

// Available LLM Models
const char* LLM_MODELS[] = {
  "gpt-4o-mini",
  "gpt-image-1", 
  "o3"
};
const int NUM_LLM_MODELS = sizeof(LLM_MODELS) / sizeof(LLM_MODELS[0]);

#endif // CONFIG_H 